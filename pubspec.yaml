name: liveness
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.4+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_mlkit_face_detection: ^0.8.0
  camera: ^0.10.4
  flutter_launcher_icons: ^0.11.0
  tflite_flutter: ^0.9.0
  path_provider: ^2.0.9
  tflite_flutter_helper:
    git:
      url: https://github.com/LeBzul/tflite_flutter_helper.git
      ref: master # branch name
  animated_toggle_switch: ^0.8.0
  intl: ^0.18.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/