name: liveness
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.4+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_mlkit_face_detection: ^0.7.0
  camera: ^0.10.4
  tflite_flutter: ^0.10.0
  path_provider: ^2.0.9
  tflite_flutter_helper:
    #path: /Users/gdrouin/Documents/development/tflite_flutter_helper
    git:
      url: https://github.com/LeBzul/tflite_flutter_helper.git
      ref: master # branch name

dependency_overrides:
  image: ^3.3.0
  google_mlkit_commons: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/